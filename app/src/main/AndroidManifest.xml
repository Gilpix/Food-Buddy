<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kulartist.foodbuddy">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/food_buddy"
        android:label="@string/app_name"
        android:roundIcon="@drawable/food_buddy"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name="com.kulartist.home.Messages"></activity>
        <activity
            android:name="com.kulartist.home.BuddyProfile"
            android:label="@string/title_activity_buddy_profile" />
        <activity android:name="com.kulartist.home.MyProfile" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/text_view_button" />

        <activity android:name="com.kulartist.home.Notification" />
        <activity android:name="com.kulartist.home.Restaurant" />
        <activity android:name="com.kulartist.home.Settings" />
        <activity android:name="com.kulartist.home.Buddies" />
        <activity android:name="com.kulartist.home.Home" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".ResetPassAuthenticate" />
        <activity android:name=".Registration" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>